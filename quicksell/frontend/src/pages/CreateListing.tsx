import React from 'react';
import { Box, Typography, Container } from '@mui/material';

const CreateListing: React.FC = () => {
  return (
    <Container>
      <Box sx={{ py: 4 }}>
        <Typography variant="h4">CreateListing Page</Typography>
      </Box>
    </Container>
  );
};

export default CreateListing;
