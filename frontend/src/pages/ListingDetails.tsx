import React from 'react';
import { Box, Typography, Container } from '@mui/material';

const ListingDetails: React.FC = () => {
  return (
    <Container>
      <Box sx={{ py: 4 }}>
        <Typography variant="h4">ListingDetails Page</Typography>
      </Box>
    </Container>
  );
};

export default ListingDetails;
