import React from 'react';
import { Box, Typography, Container } from '@mui/material';

const MyListings: React.FC = () => {
  return (
    <Container>
      <Box sx={{ py: 4 }}>
        <Typography variant="h4">MyListings Page</Typography>
      </Box>
    </Container>
  );
};

export default MyListings;
