import React from 'react';
import { Box, Typography, Container } from '@mui/material';

const LoginPage: React.FC = () => {
  return (
    <Container>
      <Box sx={{ py: 4 }}>
        <Typography variant="h4">LoginPage Page</Typography>
      </Box>
    </Container>
  );
};

export default LoginPage;
