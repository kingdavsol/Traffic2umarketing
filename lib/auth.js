const jwt = require('jsonwebtoken'); const bcrypt = require('bcryptjs'); const createToken = (id) => jwt.sign({userId: id}, 'secret-2025', {expiresIn: '30d'}); const verifyToken = (t) => { try { return jwt.verify(t, 'secret-2025'); } catch { return null; } }; module.exports = { createToken, verifyToken };